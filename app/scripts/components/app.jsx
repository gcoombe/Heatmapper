import React from 'react';
import MapContainer from './mapContainer';

class App extends React.Component {
    constructor(props) {
        super(props);
    }
    render() {
        return (
            <MapContainer />
        );
    }
}

export default App;
